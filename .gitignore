# OSS repo lives in its own GitHub repo, not in this Pages deploy
oss-projects/

# CVs / role-specific résumés — kept locally, never published.
# Anything committed here is world-readable at hellouchit.com/<filename>.
cv-*.html
Uchit_Vyas_*.pdf

# OS cruft
.DS_Store
Thumbs.db
**/.DS_Store
**/Thumbs.db

# wrangler's local CLI cache — was previously committed under
# worker/benchmark/.wrangler/ and exposed a Cloudflare account id + email.
# Not a secret token, but still shouldn't be in a public repo. Removed along
# with the rest of that directory in the worker consolidation; this stops it
# from happening again in worker/site/ or any future worker/ directory.
**/.wrangler/
